seeds: 
  - name: salestarget
    config: 
      enabled: true
      schema: L1_SEEDS
      materialized: seed
      column_types:
          StoreID: number
          SalesTarget: number
  - name: tmpseed
    config: 
      enabled: true
      schema: L1_SEEDS
      materialized: seed
      column_types:
          Id: number
          Name: varchar(20)